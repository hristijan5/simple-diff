## Spring related settings for test ##
spring:
  datasource:
    url: jdbc:h2:mem:~/${spring.application.name}-test;
    username: user
    password: pass1234!
    driver-class-name: org.h2.Driver
server:
  port: 8080